version: '3.8'

services:
  grafana:
    image: grafana/grafana
    restart: always
    ports:
      - "13000:3000"
    environment: 
      GF_SECURITY_ADMIN_USER: admin
    volumes:
        - ./.docker/grafana:/var/lib/grafana
    networks: 
      - grafana

networks:
  grafana:
    driver: bridge